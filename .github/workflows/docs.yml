name: docs
on:
  push:
    branches:
      - main
      - docs
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 📰 Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.x

      - name: 🚧 Install dependencies
        run: pip install mkdocs-material mkdocs-git-revision-date-localized-plugin mike

      - name: Set version string
        run: echo VERSION_TITLE=$(mike list | grep latest) >> $GITHUB_ENV

      - name: 🏗️ Deploy
        run: |
          git config user.name qm-bot
          git config user.email qm-bot@quaternion.media
          mike deploy --push --update-aliases ${VERSION_TITLE%\[latest\]} latest
